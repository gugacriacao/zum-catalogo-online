# Instruções para o Claude Code — Projeto ZUM Catálogo Online

## Memória do projeto
Todos os arquivos de memória ficam em `memory/` dentro desta pasta.
Quando precisar salvar ou atualizar memória, use sempre:
`/Users/tavinho/Library/Mobile Documents/com~apple~CloudDocs/ZUM/CATALOGO ZUM ONLINE/memory/`

O índice é `memory/MEMORY.md`. Atualize-o sempre que criar ou modificar um arquivo de memória.

## Contexto rápido
- Projeto: loja virtual mobile-first da ZUM (moda feminina)
- Leia `memory/project_loja_online.md` para o estado completo do projeto
- Leia `memory/project_zum.md` para contexto da marca e planilha
- Leia `RESUMO-PROJETO.md` para visão geral rápida

## Regras
- Nunca alterar a planilha Google Sheets diretamente — só leitura via Apps Script
- Entregar sempre arquivos prontos para Mac (.html, .command, texto colável)
- Salvar memória nesta pasta, não em ~/.claude/
